syntax = "proto3"; //Specify proto3 version.

package work_leave; //Optional: unique package name.

service validation_Student {
  rpc search_for_roll_no (test) returns (return_test);
}
message test{
  int32 roll_no = 1;
  string name = 2;
}
message return_test{
  bool value = 1;
}
